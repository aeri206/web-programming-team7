<section>
<% if @article.if_sub %>
    <h5>
        <%= link_to @article.title, @title_url %>&gt;
        <%= link_to @article.sub_title, @sub_url %> 
    </h5>
<% else %>
    <h5>
        <%= link_to @article.title, @title_url %>
    </h5>
<% end %>
    <br>
<header class="main">
<% if @article.if_sub %>
    <h1><%= @article.sub_title %></h1>
<% else %>
    <h1><%= @article.title %></h1>
<% end %>
</header>
<div><%= @article.content.html_safe %></div>
<h5><%= link_to '다음페이지로 이동', @next_url %></h5>
</section>



