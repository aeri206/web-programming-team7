// = require jquery3
// = require popper
// = require bootstrap-sprockets

$(function () {
    $('[data-toggle="popover"]').popover({
        html: true
    });
})

// $(document).ready(() => {
//     alert("be careful!");
// })
